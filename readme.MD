To debug: $env:DEBUG='=Eleventy*';npx @11ty/eleventy

To compile scss files: With Sass installed locally, navigate to the root of  
the site directory and run `sass --watch --style compressed  scss:css`